colorscheme paragon

" Nvim 0.9 has built-in EditorConfig support, but I use vim-sleuth right now
" (which also supports guessing appropriate settings if no such file exists).
let g:editorconfig = v:false
